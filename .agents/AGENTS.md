# Git Preferences
- **Squash Merges Only:** When merging any branch into the main/master branch, ALWAYS use a squash merge (e.g., `git merge --squash` or via GitHub PR settings) to maintain a clean and linear commit history. Never perform a standard merge commit unless explicitly requested by the user.
